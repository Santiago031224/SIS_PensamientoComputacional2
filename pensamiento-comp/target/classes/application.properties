spring.application.name=pensamiento-comp

# Server port configuration
server.port=8080

## Oracle datasource (SQL)
spring.datasource.url=${ORACLE_JDBC_URL:jdbc:oracle:thin:@//200.3.193.24:1522/ESTUD}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.username=${ORACLE_USERNAME:P09779_3_27}
spring.datasource.password=${ORACLE_PASSWORD:EFojDo3U}

# Hibernate / JPA for Oracle
spring.jpa.hibernate.ddl-auto=none
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=true
spring.flyway.schemas=${ORACLE_SCHEMA:P09779_3_27}
## Decouple Flyway from JPA by giving it its own connection to avoid bean cycles
spring.flyway.url=${ORACLE_JDBC_URL:jdbc:oracle:thin:@//200.3.193.24:1522/ESTUD}
spring.flyway.user=${ORACLE_USERNAME:P09779_3_27}
spring.flyway.password=${ORACLE_PASSWORD:EFojDo3U}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect
spring.jpa.defer-datasource-initialization=false
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

spring.thymeleaf.cache=false

# Disable SQL init scripts for Oracle (use Flyway/Liquibase or manual DDL)
spring.sql.init.mode=never

## MongoDB (NoSQL)
app.mongo.enabled=true
# Full connection string to Atlas; can be overridden by env MONGODB_URI
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://db_user_pc:pensamientoComutacional@pensamiento-computacion.aubasa6.mongodb.net/?appName=pensamiento-computacional}
spring.data.mongodb.database=${MONGODB_DATABASE:plataforma_pensamiento}


